<%= form_for(boarding_action) do |f| %>
  <% if boarding_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boarding_action.errors.count, "error") %> prohibited this boarding_action from being saved:</h2>

      <ul>
      <% boarding_action.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :trip_id %>
    <%= f.number_field :trip_id %>
  </div>

  <div class="field">
    <%= f.label :student_id %>
    <%= f.number_field :student_id %>
  </div>

  <div class="field">
    <%= f.label :onboard %>
    <%= f.check_box :onboard %>
  </div>

  <div class="field">
    <%= f.label :boarded_at %>
    <%= f.datetime_select :boarded_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
